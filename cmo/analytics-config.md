# Shared Analytics Config

Provider: Umami
Base URL (dashboard): https://cloud.umami.is
Base URL (API): https://api.umami.is/v1
Auth header: x-umami-api-key: api_<KEY> (note api_ prefix)
Secret storage: local root .env (gitignored)

Per-product website IDs should be tracked inside each product workspace.
